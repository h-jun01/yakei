import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  userIcon: {
    width: 50,
    height: 50,
    borderRadius: 180,
  },
  imageSize: {
    width: 200,
    height: 200,
  },
});
