import React, { FC } from "react";
import { ScrollView, View, Text } from "react-native";

const PostedImageDetail: FC = () => {
  return (
    <ScrollView>
      <View>
        <Text>aaaa</Text>
      </View>
    </ScrollView>
  );
};

export default PostedImageDetail;
